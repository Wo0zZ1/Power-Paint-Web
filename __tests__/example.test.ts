// Пример unit-теста с использованием Jest и React Testing Library
// Запустить: yarn test

import { render as _render, screen as _screen } from "@testing-library/react";
// import { ConnectionStatus } from '@/widgets/board/ui/overlay/ConnectionStatus';
// import { WebSocketStatus } from '@hocuspocus/provider';
// import { NextIntlClientProvider } from 'next-intl';

describe("Example Empty Test", () => {
  it("runs successfully", () => {
    expect(1 + 1).toBe(2);
  });
});
