export {
  LOCALES,
  DEFAULT_LOCALE,
  ALL_LOCALES,
  isLocaleSupported,
  getLocale,
  getMessageFallback,
} from "./config";

export type { SupportedLocaleCode, ISupportedLocale } from "./config";
